# Discord Activity credentials (see https://docs.discord.com/developers/activities/building-an-activity)
# Copy to .env in this folder: cp example.env .env

# From Developer Portal → your app → OAuth2:
VITE_DISCORD_CLIENT_ID=your_application_id
DISCORD_CLIENT_SECRET=your_client_secret

# Optional: same values with alternate names (client accepts either)
# VITE_CLIENT_ID=your_application_id
# CLIENT_SECRET=your_client_secret
